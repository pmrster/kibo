<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>CFBundleName</key><string>Kibo</string>
  <key>CFBundleDisplayName</key><string>Kibo</string>
  <key>CFBundleIdentifier</key><string>pmrster.kibo</string>
  <key>CFBundleExecutable</key><string>Kibo</string>
  <key>CFBundleIconFile</key><string>AppIcon</string>
  <key>CFBundlePackageType</key><string>APPL</string>
  <!-- Placeholders. package.sh sets both by key name with PlistBuddy: the version from its
       argument, the build number from the commit count. Editing them here has no effect on a
       packaged build. -->
  <key>CFBundleShortVersionString</key><string>0.0.0</string>
  <key>CFBundleVersion</key><string>0</string>
  <key>LSMinimumSystemVersion</key><string>14.0</string>
  <!-- Menu-bar-only: no Dock icon, no app switcher entry. -->
  <key>LSUIElement</key><true/>
  <key>NSHumanReadableCopyright</key><string>Copyright © 2026 pmrster. MIT Licensed.</string>
  <!-- The right-click → Services entry path: select text in any app, pick the item, and the
       selection is replaced with the converted text. NSMessage names the selector on
       ConversionService; NSReturnTypes is what makes it a replacement rather than a one-way
       send. The text travels on a private per-invocation pasteboard, not the general clipboard.
       Users can give it a shortcut in System Settings → Keyboard → Shortcuts → Services. -->
  <key>NSServices</key>
  <array>
    <dict>
      <key>NSMenuItem</key>
      <dict><key>default</key><string>Fix Layout with Kibo</string></dict>
      <key>NSMessage</key><string>fixLayout</string>
      <key>NSPortName</key><string>Kibo</string>
      <key>NSSendTypes</key><array><string>NSStringPboardType</string></array>
      <key>NSReturnTypes</key><array><string>NSStringPboardType</string></array>
    </dict>
  </array>
</dict>
</plist>
