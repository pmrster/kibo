namespace Kibo.Core.Tests;

/// Proves the solution builds and the runner works on this machine before any port lands.
public class SmokeTests
{
    [Fact]
    public void The_test_runner_runs()
    {
        Assert.True(true);
    }
}
